# MongoDB properties
mongoDbUri: mongodb://localhost:27017
mongoDbDatabase: securehome

# JWT secret
secret: ${JWT_SECRET:-secret}

server:
  applicationConnectors:
  - type: http
    port: 9000
  adminConnectors:
  - type: http
    port: 9001